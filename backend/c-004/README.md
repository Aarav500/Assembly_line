Frontend scaffolds (Vite/React, Next.js, Svelte, Angular) backed by a simple Flask API.

Run:
- pip install -r requirements.txt
- python app.py

Endpoints:
- GET / -> Service info
- GET /scaffold/<framework>?name=My%20App&description=Something -> Download zip

Framework keys: vite-react, nextjs, svelte, angular

